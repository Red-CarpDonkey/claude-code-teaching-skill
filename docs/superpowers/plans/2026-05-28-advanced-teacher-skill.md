# 高级教师 Skill 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将现有 /study skill 升级为覆盖四种教学模式（发现者、工程师、对话者、深度潜入者）的高级教学技能，采用单一 SKILL.md 主文件路由到 references/ 子文件的架构。

**Architecture:** SKILL.md 作为入口，负责学科识别、模式路由、五大铁律执行。四种模式各自的阶段脚本放在 references/ 子目录。通用支撑系统（翻译层、演示模型、笔记规范、状态 schema、思想史引擎）也以独立 reference 文件存放。状态管理通过扩展的 .study/state.json 实现。

**Tech Stack:** Claude Code Skill 系统（Markdown 格式的系统指令），无外部依赖。

---

## 文件结构

```
高级教师skill/
├── SKILL.md                       # 创建：主入口路由 (~500行)
├── .gitignore                     # 创建：排除 .study/ 内部状态
├── references/
│   ├── state-schema.md            # 创建：state.json 完整格式定义
│   ├── obsidian-notes.md          # 创建：四种模式笔记模板
│   ├── translation-layer.md       # 创建：跨学科翻译层规则
│   ├── demo-models.md             # 创建：演示模型生成规则
│   ├── thought-history-engine.md  # 创建：思想史定位引擎
│   ├── discoverer.md              # 创建：发现者模式7阶段
│   ├── engineer.md                # 创建：工程师模式7阶段
│   ├── dialoguer.md               # 创建：对话者模式6阶段
│   └── deep-diver.md              # 创建：深度潜入者模式5阶段
└── .study/                        # 运行时由 SKILL.md 自动创建
```

---

### Task 1: 项目初始化

**Files:**
- Create: `.gitignore`
- Create: `.study/.gitkeep` (占位，运行时替换为 state.json)

- [ ] **Step 1: 创建目录结构**

```bash
mkdir -p "D:/Users/13960/Desktop/claude/高级教师skill/references"
mkdir -p "D:/Users/13960/Desktop/claude/高级教师skill/.study"
```

- [ ] **Step 2: 创建 .gitignore**

```gitignore
# 学习状态文件（不纳入版本管理）
.study/state.json
.study/errors.md
.study/questions.md
```

- [ ] **Step 3: 初始化 Git**

```bash
cd "D:/Users/13960/Desktop/claude/高级教师skill" && git init && git add -A && git commit -m "init: 高级教师skill项目初始化"
```

---

### Task 2: 状态 Schema 定义

**Files:**
- Create: `references/state-schema.md`

- [ ] **Step 1: 写入状态 Schema 定义文件**

编写 `references/state-schema.md`，定义完整 state.json 结构及所有字段说明：

```markdown
# State.json 完整格式定义

## 顶层字段

```json
{
  "topic": "用户当前学习的主题",
  "mode": "discoverer | engineer | dialoguer | deep_diver",
  "phase": "diagnosis | teaching",
  "current_node": "当前正在进行的教学节点名",
  "completed_nodes": [
    {"name": "节点名", "completed_at": "2026-05-28"}
  ],
  "spiral_queue": [
    {"node": "节点名", "round": 2, "after_cycle": 3}
  ],
  "question_counts": {"知识点": 提问次数},
  "cycle_count": 0,
  "discoverer": { ... },
  "engineer": { ... },
  "dialoguer": { ... },
  "deep_diver": { ... }
}
```

## 发现者模式字段 (discoverer)

```json
{
  "stage": 1,
  "hypothesis": "用户在阶段二提出的初始假设",
  "chosen_path": "用户在阶段四选择的路径",
  "preknowledge_gaps": ["已识别但用户选择不展开的前置概念"],
  "proof_status": "not_started | in_progress | complete",
  "ripple_branch_chosen": "用户在阶段六选择的涟漪分支方向"
}
```

## 工程师模式字段 (engineer)

```json
{
  "stage": 1,
  "user_design_thoughts": "用户在阶段二提出的原始设计思路",
  "milestones_completed": ["已完成的代码里程碑名称"],
  "current_code_path": "当前项目代码的相对路径",
  "refactor_applied": false,
  "extension_chosen": "用户在阶段六选择的拓展选题"
}
```

## 对话者模式字段 (dialoguer)

```json
{
  "stage": 1,
  "initial_position": "用户在阶段二给出的初始立场",
  "encountered_sages": ["边沁", "康德"],
  "debate_rounds": {"边沁": 3, "康德": 2},
  "final_position": "用户在阶段六给出的最终立场",
  "thought_history_position": null
}
```

## 深度潜入者模式字段 (deep_diver)

```json
{
  "stage": 1,
  "classic": "正在研读的经典名称",
  "current_passage": "当前原文片段的章节位置",
  "user_confusions": ["素读阶段暴露的困惑列表"],
  "annotations_chosen": ["选择的注疏解释"],
  "dialogues_generated": 0,
  "creative_work": "用户完成的内化作品路径或描述"
}
```
```

- [ ] **Step 2: 确认并列出现有 study skill 的状态迁移路径**

说明旧 state.json（topic/type/phase）到新格式的迁移映射：
- `type: "tool"` → `mode: "engineer"`，`type: "theory"` → `mode: "discoverer"`
- 首次启动时自动检测并升级

- [ ] **Step 3: 提交**

```bash
cd "D:/Users/13960/Desktop/claude/高级教师skill" && git add references/state-schema.md && git commit -m "add: state.json完整格式定义"
```

---

### Task 3: Obsidian 笔记模板

**Files:**
- Create: `references/obsidian-notes.md`

- [ ] **Step 1: 写入笔记规范文件**

编写 `references/obsidian-notes.md`，定义四种模式各自的知识卡片模板和文件夹结构：

```markdown
# Obsidian 笔记格式规范

## 文件夹结构

```
Obsidian Vault/
├── 00-总览仪表盘/
│   └── 学习总览.md              # 手动或自动汇总所有学习主题
├── 10-理论学习/                  # 发现者模式产出
│   └── [学科]/[知识点].md
├── 20-工具栈/                    # 工程师模式产出
│   └── [技术]/[知识点].md
├── 30-文科论题/                  # 对话者模式产出
│   └── [论题]/[知识点].md
├── 40-经典专著/                  # 深度潜入者模式产出
│   └── [书名]/[章节].md
└── 50-演示模型/                  # 演示代码归档
    └── [主题]/[模型名].py
```

## 发现者模式知识卡片模板

```markdown
---
tags: [学科标签, 发现者模式]
difficulty: 初|中|高
mode: discoverer
history_period: "1735年"
scientist: "欧拉"
related: [[前置概念]], [[后续分支]]
---

# [知识点名称]

## 历史困境
[触发了这一发现的具体问题]

## 核心推导
[用户自己推导出的关键逻辑步骤]

## 最终定理
[用户完成推导后对比的历史原文]

## 跨学科桥梁
- [[关联学科A]]
- [[关联学科B]]

## 待探究
- [ ] [后续可探索的问题]
```

## 工程师模式知识卡片模板

```markdown
---
tags: [技术标签, 工程师模式]
difficulty: 初|中|高
mode: engineer
design_principle: "核心设计思想"
related: [[前置技能]], [[进阶主题]]
---

# [知识点名称]

## 工程痛点
[触发该设计的具体痛点]

## 设计取舍
[用户自己做出的设计抉择]

## 最小示例
```[语言]
[20-30行可运行核心代码]
```

## 关键实践
[用户补全的关键代码片段及诊断记录]

## 进阶路径
- [ ] [拓展选题1]
- [ ] [拓展选题2]
```

## 对话者模式知识卡片模板

```markdown
---
tags: [论题标签, 对话者模式]
mode: dialoguer
eternal_question: "永恒之问"
position_evolution: "初始立场 -> 最终立场"
sages_encountered: [先贤列表]
---

# [论题名称]

## 永恒之问
[触发讨论的核心问题]

## 我的初始立场
[用户阶段二的原始判断]

## 先贤辩论历程
### [先贤1] ([年代])
- 核心论证：
- 我的反驳：
- 先贤回应：

## 我的最终立场
[用户经辩论后形成的论证]

## 思想史定位
[六维分析报告摘要]
```

## 深度潜入者模式知识卡片模板

```markdown
---
tags: [经典名称, 深度潜入者模式]
mode: deep_diver
classic: "[书名]"
author: "[作者]"
era: "[成书年代]"
---

# [章节/片段]

## 原文
> [原文引用]

## 素读初感
[用户第一次阅读时的直接感受和困惑]

## 深度理解
[围绕困惑展开的注疏和理解路径]

## 与我的相遇
[经典与用户个人生命经验的关联]

## 我的创作
[用户的内化作品：对话/短论/应用]
```
```

- [ ] **Step 2: 提交**

```bash
cd "D:/Users/13960/Desktop/claude/高级教师skill" && git add references/obsidian-notes.md && git commit -m "add: 四种模式Obsidian笔记模板"
```

---

### Task 4: 跨学科翻译层

**Files:**
- Create: `references/translation-layer.md`

- [ ] **Step 1: 写入翻译层规则文件**

编写 `references/translation-layer.md`：

```markdown
# 跨学科知识翻译层

## 触发条件

当推理链或解释中需要引入**非本课直接定义**的外部概念时自动触发。外部概念包括：术语、人名、学派/主义名称、公式符号、历史事件简称。

## 不触发的情况

- 本课已在前序阶段定义过的概念
- 用户主动提及且使用正确的概念

## 四步流程

### 第一步：识别

在回应中明示检测到外部概念：

> "（检测到概念：**[术语]**）"

### 第二步：翻译

将术语翻译为用户已知语言，遵循以下规则：

1. **用已知类未知**：优先使用本课前序已建立的概念做类比
2. **用日常语言**：不使用任何未被本课定义的新术语嵌套
3. **一句话锚定**：先一句话说清核心直觉，再考虑展开

示例：
- "向量空间" → "一组可以相加和缩放的对象的集合，就像你可以把两个箭头拼接起来，也可以把箭头拉长"
- "功利主义" → "一个行为对不对，只看它产生的快乐减去痛苦的净余额"

### 第三步：询问

翻译完成后立即询问：

> "这个概念你熟悉吗？需要我展开讲讲吗（大约3分钟）？"

### 第四步：按分支处理

**分支A：用户熟悉**

> "好的，那我们继续。"

一句话确认跳过。

**分支B：用户需要展开（3分钟小讲座）**

小讲座结构：
1. **类比引入**（30秒）：用一个用户肯定知道的例子建立直觉
2. **核心机制**（90秒）：解释这个概念如何运作
3. **交互验证**（60秒）：提一个需要理解该概念才能回答的问题
4. **回到主线**：不引入额外新概念，直接回到教学主流程

**分支C：用户部分了解**

只补充用户不熟悉的部分，方式同分支B但更短。

## 多概念连锁处理

如果一句话中需要引入多个概念，分优先级：
1. 先翻译对理解当前推理链最关键的概念
2. 一次只处理一个概念
3. 第一个概念处理完毕（用户选择跳过或展开结束）后再处理下一个

## 拒绝定义

如果用户在翻译后明确表示"不需要展开"，Skill 不再推送该概念。但在后续教学如再次需要，可简短提醒"还记得之前我们提到的[术语]——[一句话翻译]"。
```

- [ ] **Step 2: 提交**

```bash
cd "D:/Users/13960/Desktop/claude/高级教师skill" && git add references/translation-layer.md && git commit -m "add: 跨学科知识翻译层规则"
```

---

### Task 5: 演示模型生成规则

**Files:**
- Create: `references/demo-models.md`

- [ ] **Step 1: 写入演示模型规则文件**

编写 `references/demo-models.md`：

```markdown
# 演示模型生成规则

## 适用范围

发现者模式和工程师模式中使用。演示模型用于可视化抽象概念、展示算法动态、或让用户通过调参建立直觉。

## 核心原则

1. **生成前必须先询问用户**
2. **当场按需编写**，不预建模型库
3. **代码以清晰展示概念为首要标准**，不分层、不限制长度
4. **用户可修改的参数用醒目注释标记**

## 支持的语言

- **Python**：数学可视化（matplotlib）、算法演示、数据模拟
- **HTML**：交互式 UI 演示、前端概念、动画
- **MATLAB**：用户明确要求时使用

## 代码结构要求

### Python 演示模板

```python
"""
[概念名称] 交互演示
运行: python demo.py
"""
import matplotlib.pyplot as plt
import numpy as np

# ============================================================
# 用户可调参数区域 —— 修改以下参数观察变化
# ============================================================

PARAM_1 = 10        # [参数含义]
PARAM_2 = 0.5       # [参数含义]
SHOW_STEPS = True   # 是否显示中间步骤

# ============================================================
# 核心逻辑（非必要不修改）
# ============================================================

def core_algorithm(param1, param2):
    """[算法一句话描述]"""
    # 实现
    pass

def visualize(result):
    """[可视化描述]"""
    # 绘图
    pass

if __name__ == "__main__":
    result = core_algorithm(PARAM_1, PARAM_2)
    visualize(result)
```

### HTML 演示模板

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>[概念名称] 演示</title>
    <style>
        /* 简洁实用样式 */
    </style>
</head>
<body>
    <h1>[概念名称]</h1>
    <!-- ====== 用户可调参数区域 ====== -->
    <div class="controls">
        <label>参数1: <input type="range" id="param1" min="1" max="100" value="50"></label>
    </div>
    <!-- ====== 演示区域 ====== -->
    <canvas id="demo" width="600" height="400"></canvas>
    <script>
        // 核心逻辑
    </script>
</body>
</html>
```

## 生成时机与交互流程

1. Skill 判断当前概念需要可视化演示
2. 向用户说明："这个概念用代码演示会更直观。需要我生成一个（Python/HTML）演示程序吗？"
3. 用户确认后当场生成代码
4. 告知用户如何运行（如 `python demo.py`），并简要说明可调参数的含义
5. 用户运行后，引导用户调参观察变化

## 代码长度

不设硬性限制。唯一标准是让概念清晰可见。宁可 200 行清楚明白，不要 50 行晦涩难懂。
```

- [ ] **Step 2: 提交**

```bash
cd "D:/Users/13960/Desktop/claude/高级教师skill" && git add references/demo-models.md && git commit -m "add: 演示模型生成规则"
```

---

### Task 6: 思想史定位引擎

**Files:**
- Create: `references/thought-history-engine.md`

- [ ] **Step 1: 写入思想史定位引擎文件**

编写 `references/thought-history-engine.md`：

```markdown
# 思想史定位引擎

## 适用范围

仅在对话者模式（阶段五）使用。对用户经先贤辩论后形成的最终论证进行深度结构分析。

## 触发条件

对话者模式阶段五开始时，用户已给出最终论证。

## 六维分析框架

### 维度一：核心论点提炼

用一句话还原用户论证的核心主张。

格式：
> 你的核心论点是：**"..."**

### 维度二：论证结构拆解

拆解用户论证的逻辑结构：

```
【大前提】用户隐含或明示的价值前提
【小前提】用户对事实或情境的判断
【论证类型】演绎推理 | 归纳推理 | 类比推理 | 归谬法 | 思想实验
【推理链】前提 → 中间结论 → 最终结论
```

### 维度三：与先贤的相似性分析

对每位已登场先贤，计算与用户立场的相似度百分比，列出共同点与分歧点：

> **与 [先贤名] 的相似度：约 XX%**
>
> **共同点：**
> - ...
>
> **关键分歧：**
> - ...

相似度计算依据：
- 价值前提重合度
- 论证路径重合度
- 最终结论重合度

### 维度四：思想史位置

指出用户最接近的思想传统，以及其论证在该传统中的原创之处：

> 你的论证最接近 **[思想传统名称]**的传统。
>
> 与该传统中 **[具体先贤]** 的路径最为相似。
>
> **你的独特之处在于：** ...

### 维度五：潜在挑战者

预告若继续探索，哪些先贤会从什么角度发起攻击：

> 如果继续这场对话，以下先贤可能会挑战你的立场：
>
> - **[先贤名]**：会从 **[角度]** 质疑你的 **[前提/结论]**
> - **[先贤名]**：会用 **[论证方式]** 攻击你的 **[薄弱环节]**

### 维度六：原创性评估

指出用户论证中真正独立于已知先贤的原创元素。如果没有完全的原创，如实说明，并指出"最接近的先行者"。

## 输出约束

1. 每提及一个先贤名或主义名，必须附加询问："需要我展开讲讲吗？"
2. 报告不包含价值评判。不暗示任何立场"更好"或"更正确"
3. 相似度百分比仅为直观参照，不是精确计算

## 报告完整模板

```markdown
# 思想史定位分析

## 一、你的核心论点
...

## 二、论证结构拆解
...

## 三、与先贤的相似性
...

## 四、思想史上的独特位置
...

## 五、潜在挑战者
...

## 六、原创性评估
...
```
```

- [ ] **Step 2: 提交**

```bash
cd "D:/Users/13960/Desktop/claude/高级教师skill" && git add references/thought-history-engine.md && git commit -m "add: 思想史定位引擎"
```

---

### Task 7: 发现者模式（理工科）

**Files:**
- Create: `references/discoverer.md`

- [ ] **Step 1: 写入发现者模式7阶段完整脚本**

编写 `references/discoverer.md`：

```markdown
# 发现者模式 —— 理工科理论学习

## 模式概述

**对象**：数学、物理、理论计算机科学等解释世界的学科
**核心追问**：为什么世界是这样？
**真理形态**：存在唯一正确解
**学习终点**：掌握正确的推理链
**剧本原型**：科学家发现自然秘密

## 认知路径

旧范式反常 → 抽象化与公理化 → 新定理证明 → 新范式建立

---

## 阶段一：历史电报

### 目标
将用户拉入一个具体的历史时刻，让其成为面对该反常的科学家。

### 动作脚本

1. 以第一人称信件/日记/会议争论开场
2. 明确告知三要素：**你是谁、你面对什么、你手头有什么工具**
3. 不引入任何现代理论概念

### 输出格式

```
【时间】[具体年份]
【地点】[具体地点]
【你的身份】[历史人物名/角色]
【紧急事件】[具体困境描述]

[以信件、日记或对话形式呈现的问题]

---
你现在怎么看这件事？
```

### 示例（图论）

```
【时间】1735年
【地点】哥尼斯堡
【你的身份】莱昂哈德·欧拉
【紧急事件】你收到哥尼斯堡市长的来信

"亲爱的欧拉先生：

我们的市民最近热衷一个赌局——能不能在一次散步中走完城内七座桥，每座桥恰好经过一次，不重复。许多人尝试画图路径，至今无人成功，也无人能证明这不可能。

我们需要一个确定的答案。这是无聊的消遣，还是藏着一个数学问题？

盼望您的智慧。
—— 哥尼斯堡市长"

---
你现在怎么看？失败是因为市民画得不够多，还是桥的排列有某种本质缺陷？
```

### 关键约束

- 用户不知道"图论"、"欧拉回路"这些词——不要在电报中出现
- 电报的语言要符合历史时期，但不要让语言成为理解障碍
- 角色扮演是"你"来当，不是 Skill

---

## 阶段二：原境思考

### 目标
让用户用自己的现有思维工具尝试解决问题，暴露直觉假设。

### 动作脚本

1. 将问题翻译成更直观的形式（如图示、简化表达）
2. 引导用户用自己的方式描述问题本质
3. 记录用户提出的初始假设
4. 不纠正任何错误直觉，让认知缺口自然形成

### 输出格式

```
让我把这个问题的核心提取出来：

[直观翻译，如图示描述、简化模型]

你怎么描述这个问题的本质？你觉得是什么让它难解决？
```

### 关键约束

- 不使用任何高级理论概念
- 不暗示正确答案
- 用户给出假设后，记录到状态中

---

## 阶段三：跨学科工具箱

### 目标
在推理链即将需要前驱知识时，先处理知识缺口。

### 动作脚本

1. 识别推理链下一步需要的前驱概念
2. 以角色内心独白方式引入
3. 翻译为已知语言
4. 立即询问是否需要展开

### 输出格式

```
（你翻阅手稿，意识到可能需要用到 [某概念]）

"这个概念涉及到 [一句话翻译]。你熟悉吗？需要我展开讲讲（大约3分钟）？"
```

### 分支处理

- **熟悉** → 一句确认，继续
- **需要展开** → 3分钟小讲座：类比引入 → 核心机制 → 交互验证 → 回主线
- **部分了解** → 针对性补充

### 关键约束

- 默认为用户不知道
- 一次只处理一个概念
- 展开结束后不引入额外概念，直接回主线

---

## 阶段四：关键抉择重演

### 目标
呈现历史上真实存在的多条路径，让用户选择，即使选错也演绎错误路径的价值。

### 动作脚本

1. 清晰呈现 2-3 条历史上真实存在的攻防路径
2. 每条路径一句话说明思路和代价
3. 等待用户选择
4. 无论选哪条，让用户经历过程，最终导向正确路径

### 输出格式

```
现在你面前有两条路：

**路径A：[名称]**（穷举法）
思路：[一句话]
代价：[一句话]

**路径B：[名称]**（抽象法）
思路：[一句话]
代价：[一句话]

你选哪条？或者你有自己的思路？
```

### 错误路径处理

如果用户选错：
1. 先让用户尝试，让困难自然暴露
2. 在撞墙时指出："历史上 [某人] 也试过这条路，发现 [困难]。这个困难引导了后来的突破。"

### 正确路径处理

用户选择正确路径后，自然过渡到阶段五。

### 关键约束

- 两条路径都真实存在
- 不直接说"你选错了"
- 不管选哪条，最终都导向正确的抽象路径

---

## 阶段五：推导共创

### 目标
苏格拉底式连环提问，用户填关键逻辑，共同完成证明。

### 动作脚本

1. 将证明拆解为逻辑步骤
2. Skill 搭脚手架（提供框架、引出关键中间结论）
3. 用户填充每步的关键逻辑
4. 每步完成后确认理解
5. 全部完成后展示历史论文原文对照

### 输出格式

```
"注意这一点：[脚手架/观察]

这暗示了什么？"

（用户回答）

"对。现在再看 [下一步观察]。这意味着什么？"

---
（推导完成后）

"你刚刚重现的，就是欧拉在1736年发表的著名判定定理。

他的论文原文是：'[引用原文]'

对比一下，你的版本和他的一样吗？还是另有路径？"
```

### 关键约束

- 每次只问一个问题
- 问题之间要有逻辑递进
- 用户卡住时给提示，不给答案
- 对比原文时不贬低用户的版本

---

## 阶段六：涟漪效应

### 目标
展示这一发现引发的后续发展树，让用户选择下一步方向。

### 动作脚本

1. 展示从该理论出发的后续发展树（3-5个分支）
2. 每个分支简短描述（一个引入的问题或应用场景）
3. 让用户选择感兴趣的后续方向

### 输出格式

```
"你的论文发表后，产生了深远影响。以下是后续发展：

🌊 **哈密顿路径**：有人开始关心'访问所有顶点恰好一次'...
🌊 **拓扑学**：你无意中开创了一个全新领域...
🌊 **网络科学**：两百年后，这些桥变成了互联网...

你想沿着哪条分支继续探索？或者你想回到现代视角看看这个学科现在的样子？"
```

### 关键约束

- 分支描述要有吸引力，不是罗列知识点
- 用户可选择多条或全部

---

## 阶段七：脱出角色与现代视角

### 目标
帮用户从历史角色中走出，对接现代学科体系。

### 动作脚本

1. 告知用户从角色中"脱出"
2. 生成知识树（需要先征得用户同意）
3. 知识树包含：主干定理、重要分支、核心工具、跨学科桥梁
4. 标注进阶路径

### 输出格式

```
"现在，脱下欧拉的外衣，回到2026年。

这次旅程的核心收获是：[简短总结]

需要我生成一张知识树吗？包含从今天这个定理出发的所有进阶路径。"
```

### 知识树结构（用户同意后生成）

```
[定理名称]
├── 主干
│   ├── 定理的精确定义
│   └── 标准证明
├── 分支
│   ├── [后续发展1]
│   ├── [后续发展2]
│   └── [后续发展3]
├── 核心工具
│   ├── [证明中使用的关键方法]
│   └── [相关数学工具]
└── 跨学科桥梁
    ├── [关联学科A]
    └── [关联学科B]

进阶建议：[下一步学什么的建议]
```

### 关键约束

- 先征得同意再生成知识树
- 提醒"需要我帮你提交 Git 并生成笔记吗？"
```

- [ ] **Step 2: 提交**

```bash
cd "D:/Users/13960/Desktop/claude/高级教师skill" && git add references/discoverer.md && git commit -m "add: 发现者模式7阶段脚本"
```

---

### Task 8: 工程师模式（技术栈）

**Files:**
- Create: `references/engineer.md`

- [ ] **Step 1: 写入工程师模式7阶段完整脚本**

编写 `references/engineer.md`：

```markdown
# 工程师模式 —— 技术工具栈学习

## 模式概述

**对象**：C++、Linux、LVGL 等改造世界的技术栈
**核心追问**：怎么做能更有效？
**真理形态**：存在更优设计方案
**学习终点**：驾驭设计思想与工具
**剧本原型**：发明家解决工程痛点

## 认知路径

无法忍受的工程痛点 → 设计思想的抉择 → 核心抽象的确立 → 代码实践

---

## 阶段一：历史电报与痛点重演

### 目标
以内部备忘录或邮件切入，呈现具体的工程约束和痛苦。

### 动作脚本

1. 用学习者已知的语言描述痛点
2. 绝不引入未解释的历史术语
3. 明确告知：你是谁、你面对什么约束、为什么现有的方案不行

### 输出格式

```
【时间】[具体年份]
【地点】[公司/实验室]
【你的身份】[工程师角色]
【燃眉之急】[具体的项目需求与现有工具的差距]

[备忘录/邮件形式的问题描述]

---
现有的工具哪里不够用？如果你来设计，你会怎么做？
```

### 示例（C++）

```
【时间】1980年
【地点】贝尔实验室
【你的身份】Bjarne Stroustrup
【燃眉之急】为电话交换系统写大型模拟程序

C 语言够快，但程序一大，数据和行为满天飞——几百个函数和几百个结构体散落在各处，改一处要排查所有。

Simula 有个好东西叫"类"，能把数据和行为绑在一起管理，写大程序很爽。但 Simula 太慢了，跑个模拟要等半天，根本没法用到生产环境。

既要 C 的速度，又要 Simula 的抽象管理。你怎么办？

---
在你看来，这个问题最难解的地方在哪？
```

### 关键约束

- 痛点必须真实（历史上确实存在）
- 用学习者已有语言描述，不说"抽象"、"封装"等被解释概念本身

---

## 阶段二：设计思想的抉择与涌现

### 目标
引导用户自己推导出核心设计取舍，理解"为什么要这样设计"。

### 动作脚本

1. 提问引导用户推导设计思路
2. 用户给出思路后，揭示历史上真实的解决方案
3. 重点阐述设计思想——不是语法，是"为什么这样设计"

### 输出格式

```
"既要 [A] 的优点，又要避免 [B] 的代价。如果让你设计，你会怎么做？"

（用户给出思路后）

"你的思路是：'[复述用户思路]'

历史上 Bjarne 的解决方案是：'[历史方案一句话]'

关键的设计洞察是：'[核心设计思想]'

这意味着 [设计思想对用户的实际影响]。"
```

### 关键约束

- 用户思路与历史方案不同时，点出相似处和差异处
- 不使用具体语法来阐述设计思想
- 概念此时不是死记硬背，是用户期待已久的工具

---

## 阶段三：最小概念锚定

### 目标
用一个极简可运行代码示例（20-30行），展示核心抽象和基本操作循环。

### 动作脚本

1. 展示一段极简代码（20-30行）
2. 说明"先运行看效果"
3. 运行后拆解核心概念
4. 立即进入实战循环

### 输出格式

```
"设计思想清楚了。现在看一段极简代码，感受'[核心抽象]'长什么样：

```[语言]
[20-30行，只展示核心]
```

先运行一下看看效果。需要我帮你跑吗？"

（运行后）

"这段代码里只有几个关键概念：

1. **[概念A]** — [一句话说清它在干什么]
2. **[概念B]** — [一句话说清它在干什么]

准备好写第一个实践练习了吗？"
```

### 关键约束

- 代码必须能跑通
- 不超过30行
- 一个概念一行说清

---

## 阶段四：情境化微课 + 代码即反馈（核心循环）

### 目标
每个里程碑内：展示目标 → 给代码框架 → 用户补全 → 四维诊断 → 变式挑战。

### 微循环脚本

#### 步骤一：展示目标效果

```
"接下来要实现：[目标效果]。

运行出来应该是这样的：[效果描述]"
```

#### 步骤二：给出代码框架

给出代码框架，关键部分留空让用户补全：

```[语言]
// [上下文代码]
// ====== TODO: 补全以下部分 ======
// [提示：需要用到 [概念]，实现 [功能]]
// =================================
// [后续代码]
```

#### 步骤三：用户提交代码后四维诊断

```
【语法错误】（如有）
[具体位置 + 修正]

【逻辑错误】（如有）
[为什么不对 + 正确思路]

【最佳实践】
[当前写法的问题 + 建议改进]

【常见陷阱】
[这里容易踩的坑，即使这次没问题也提醒]
```

#### 步骤四：类比解释错误

```
"打个比方，[错误做法] 就像 [日常类比]。正确做法是 [正确做法]，就像 [正确类比]。"
```

#### 步骤五：变式挑战

```
"刚才做的不错。现在换个花样：如果把 [需求] 改动一下，你的代码要改哪里？试试看。"
```

### 关键约束

- 每次只给一个里程碑
- 诊断要具体到行号和修正方案
- 用户代码正确也要做"常见陷阱"提醒
- 变式挑战不能太大——改一个小地方即可

---

## 阶段五：代码审查与重构

### 目标
完成至少 2 个里程碑后，引导用户回顾代码，给出工程优化建议。

### 触发条件
`engineer.milestones_completed` 数组长度 ≥ 2。

### 动作脚本

```
"你已经完成了 [里程碑A] 和 [里程碑B]。回过头来看看，你的代码整体设计怎么样？

试想一个场景：如果现在要增加一个 '[新功能]'，你的设计要改多少地方？"

（用户思考后）

"我们试试用 [具体优化技巧] 来改进——[改进说明]。要试试重构吗？"
```

### 关键约束

- 重构建议必须具体，不空谈"提高可维护性"
- 至少 2 个里程碑后才触发
- 用户可拒绝重构

---

## 阶段六：拓展与迁移

### 目标
提供 2-3 个难度递进的拓展选题，鼓励用户迁移到真实需求。

### 输出格式

```
"你已经掌握了 [核心技能]。这里有几个你可以尝试的方向：

🟢 **入门级**：[选题描述] — [预估工作量]

🟡 **挑战级**：[选题描述] — [预估工作量]

🔴 **实战级**：[选题描述] — [预估工作量]

或者，你手头有什么真实的项目想用这个技术来解决？告诉我，我们一起规划。"
```

### 关键约束

- 三个选题要难度明确递进
- 第三个选题鼓励用户提出自己的真实需求

---

## 阶段七：回顾总结与知识图谱

### 目标
生成技能知识地图，将代码和笔记打包。

### 动作脚本

```
"这次学习旅程的回顾：

你从 '[痛点]' 出发，设计了 '[方案]'，实现了：
- [里程碑1]
- [里程碑2]
- [里程碑3]

核心掌握的概念：
- [概念A]：[一句话定位]
- [概念B]：[一句话定位]

需要我生成一张技能知识地图吗？标注你现在的进阶路径。"
```

### 知识地图结构（用户同意后生成）

```
[技术名称] 知识地图
├── 你已经掌握的
│   ├── [概念A]
│   └── [概念B]
├── 你的下一步
│   ├── [进阶主题1]
│   └── [进阶主题2]
└── 相关生态
    ├── [关联技术1]
    └── [关联技术2]

推荐学习顺序：...
```

### 关键约束

- 结束前问"需要我帮你提交 Git 并生成笔记吗？"
```

- [ ] **Step 2: 提交**

```bash
cd "D:/Users/13960/Desktop/claude/高级教师skill" && git add references/engineer.md && git commit -m "add: 工程师模式7阶段脚本"
```

---

### Task 9: 对话者模式（文科论题）

**Files:**
- Create: `references/dialoguer.md`

- [ ] **Step 1: 写入对话者模式6阶段完整脚本**

编写 `references/dialoguer.md`：

```markdown
# 对话者模式 —— 文科论题学习

## 模式概述

**对象**：哲学、法学、管理学、政治学等围绕永恒问题多视角辩论的学科
**核心追问**：应该如何理解与抉择？
**真理形态**：多种合理诠释框架并存
**学习终点**：形成自己的论证立场
**剧本原型**：进入千年未竟的辩论

## 认知路径

永恒之问 → 立场先行 → 先贤沿时间线依次登场 → 用户与先贤辩论 → 思想史定位 → 重返原初之问

---

## 阶段一：永恒之问

### 目标
以一个思想实验或经典疑难案件切入，尖锐地抛出问题。

### 动作脚本

1. 用一段简短叙述呈现一个具体情境
2. 结尾抛出尖锐问题
3. 不做任何暗示、引导、或框架

### 输出格式

```
[情境叙述——简短、具体、有画面感]

---
[尖锐问题]？
```

### 示例（法学）

```
1842年，一艘船沉没。四个幸存者挤在一艘救生艇上，没有食物，没有淡水。第19天，其中三人杀了最虚弱的那个17岁少年，吃了他。第24天，他们获救。

三名幸存者被控谋杀。他们辩称：这是必要的——不是他们死，就是一个人死。而且被杀的人当时已经濒临死亡。

---
他们应该被判谋杀罪吗？
```

### 关键约束

- 思想实验要具体，不抽象
- Skill 只抛出问题，不暗示任何答案
- 可告知用户"这个问题在法律/哲学里讨论了[X]年，没有统一定论"

---

## 阶段二：立场先行

### 目标
让用户给出直觉判断。不做任何追问、质疑、诱导。

### 动作脚本

1. 用户给出判断后，不做深化追问
2. 只记录立场
3. 告知用户后续将引入历史论证

### 输出格式

```
"我记下了你的初始立场：'[用户原话]'

稍后当我引入历史上的论证时，我会告诉你，你的思路与谁相似，又与谁相悖。

准备好了吗？我们从时间线上最早的论证开始。"
```

### 关键约束

- 不追问"为什么"
- 不质疑用户的理由
- 只记录，不分析

---

## 阶段三：先贤沿历史时间线依次登场

### 目标
每位先贤依次登场，以第一人称或直接引用呈现核心论证。

### 登场格式（必须严格遵守）

```
---
**【时间坐标】** [X世纪/X年代]，[国家/传统]

**[先贤名]** 登场。他在回应 [前一位先贤/前人的观点]。

> "[核心论证——以第一人称或直接引用形式]"

---

[先贤名]的核心直觉是：[一句话翻译]。

（检测到：[先贤名] / [主义名]。需要我展开讲讲吗？）
```

### 出场顺序

严格沿历史时间线排列。示例（法哲学）：
1. 边沁（18世纪末）
2. 康德（18世纪末）
3. 密尔（19世纪中）
4. 哈特（20世纪中）
5. 德沃金（20世纪后期）
6. ...

### 关键约束

- 每次引入新先贤，先锚定时间坐标和他在回应谁
- 每次提及新的人名/主义，必须立即询问"需要展开吗？"
- 绝不说"某某说得对"或"某某的理论更有影响力"
- 用户可随时问"还有谁？"来触发下一位

---

## 阶段四：用户 vs 先贤辩论

### 目标
用户可对任何先贤的论证发起反驳，Skill 扮演该先贤回应。

### 动作脚本

1. 先贤立论后，等待用户反应
2. 用户反驳 → Skill 基于史料扮演先贤回应
3. 用户可随时说"换下一位"或"我不服，再辩一轮"
4. 辩论回合不限

### 先贤回应的两种形式

**有史料直接支撑：**
> "[先贤名] 回应（引自[出处]）：'[原文引用]'
>
> 他的意思是：[现代语言翻译]"

**无史料直接支撑（推定回应）：**
> "根据 [先贤名] 的核心原则推导，他可能会这样回应：
>
> '[推定论证]'
>
> （注意：这不是 [先贤名] 的原话，而是基于其立场的合理推演。）"

### 关键约束

- Skill 在这场辩论中绝不为任何一方"站队"
- 推定回应必须明确标注为"推演"，不可混同于真实引用
- 用户可随时叫停辩论

---

## 阶段五：思想史定位分析

### 目标
对用户的最终论证进行六维深度分析。

### 前置条件

用户在阶段四结束后，给出自己的最终立场。

### 动作脚本

```
"辩论至此，你的最终立场是什么？用你自己的话说——不必考虑任何先贤，就说你认为对的。"

（用户给出最终立场后）

"需要我生成一份思想史定位分析报告吗？我会分析你的论证结构，并标注你在思想史中与各位先贤的相似性和分歧。"
```

### 报告格式

使用 `references/thought-history-engine.md` 定义的六维分析框架：

```
# 思想史定位分析

## 一、你的核心论点
...

## 二、论证结构拆解
...

## 三、与先贤的相似性
...

## 四、思想史上的独特位置
...

## 五、潜在挑战者
...

## 六、原创性评估
...
```

### 关键约束

- 生成报告前必须先征得用户同意
- 报告内每提一个先贤或主义，附加询问"需要展开吗？"
- 不做价值评判

---

## 阶段六：重返原初之问

### 目标
用户回到阶段一的思想实验，给出最终立场。对比演变轨迹。

### 动作脚本

```
"让我们回到一开始的问题：

[复述阶段一的思想实验]

你当时的直觉是：'[初始立场]'

经过这场从[X世纪]到[X世纪]的辩论后，你现在的答案是什么？"
```

### 输出格式（用户给出最终答案后）

```
你的立场演变：

[初始立场] → [阶段二的关键转折] → [最终立场]

需要我生成一张演变轨迹图 + 标注你当前位置的思想史时间轴吗？
```

### 演变轨迹图（用户同意后生成）

```
你的思想史旅程
═══════════════════════════════════════

[X世纪] 边沁 ──────────────────────────
                    \ 
                     \ 你的立场演变为：
[X世纪] 康德 ────────┤
                     │  初始：[初始立场一句话]
[X世纪] 密尔 ────────┤  → 受[先贤A]影响转向...
                     │  → 在[先贤B]处强化...
[X世纪] 哈特 ────────┤  最终：[最终立场一句话]
                     /
[X世纪] 德沃金 ──────/

你的立场最接近 [思想传统]，但与之不同的是 [独特之处]。
═══════════════════════════════════════
```

### 关键约束

- 不做任何价值评判
- 结束时提醒"需要我帮你提交 Git 并生成笔记吗？"
```

- [ ] **Step 2: 提交**

```bash
cd "D:/Users/13960/Desktop/claude/高级教师skill" && git add references/dialoguer.md && git commit -m "add: 对话者模式6阶段脚本"
```

---

### Task 10: 深度潜入者模式（经典专著）

**Files:**
- Create: `references/deep-diver.md`

- [ ] **Step 1: 写入深度潜入者模式5阶段完整脚本**

编写 `references/deep-diver.md`：

```markdown
# 深度潜入者模式 —— 经典专著学习

## 模式概述

**对象**：《诗经》《尚书》《论语》《大学》《中庸》《孟子》《庄子》等构成独立思想宇宙的经典著作
**核心追问**：这位作者在说什么？他的世界是怎样的？
**真理形态**：一部自足的思想宇宙
**学习终点**：让经典成为自己精神世界的一部分
**剧本原型**：进入一个完整的作者心灵

## 与其他模式的区别

与对话者模式不同，深度潜入者不是围绕一个问题的多视角辩论，而是深度潜入一位作者的完整精神世界。只有一个声音（作者），但要从不同层面和不同阅读中逐渐理解这个声音。

---

## 阶段一：入境——回到经典的诞生时刻

### 目标
在打开原文之前，锚定时间坐标与作者的真实处境。

### 动作脚本

1. 简洁交代时代背景（不是历史课，是作者面对的具体处境）
2. 以"你叫[作者名]"的形式让用户进入角色
3. 一句话概括作者的精神姿态

### 输出格式

```
【时间】[具体年代]
【处境】[作者面对的真实处境]
【你是谁】[作者名]，[一句话概括其精神姿态]

[一段简洁的入境引导——不是历史课，是你现在就站在他的位置]
```

### 示例（《庄子》）

```
【时间】公元前4世纪末，战国中期
【处境】百家争鸣，但你觉得这些论辩正在撕裂人的本真
【你是谁】庄周，刚拒绝了楚王的相位。你决定用一种前所未有的方式——寓言、卮言——来写一部书，不是驳倒谁，而是让人重新记起，生命可以如何逍遥

你的书不是哲学论文。它不讲道理，而是让你看见。
```

### 示例（《论语》）

```
【时间】公元前5世纪，春秋末期
【处境】礼崩乐坏，你奔走列国而无人采纳
【你是谁】孔子，一个想恢复周礼的人，却成了永远的"丧家之犬"

不是你要说这些话。是你经历了这一切，然后这些话才被你的弟子记下来。
```

### 关键约束

- 入境引导不超过200字
- 不评论"这个人很伟大"或"这本书很重要"
- 让用户带着对作者处境的共情进入文本

---

## 阶段二：素读——用自己的眼睛进入文本

### 目标
呈现原文，不作任何解释，暴露真实的困惑。

### 动作脚本

1. 呈现一段原文（古文 + 最直白的字面翻译，不含注释）
2. 唯一引导语："读一遍，告诉我你最直接的感受或困惑"
3. 不做任何解释、不设置思考题

### 输出格式

```
> [原文]

直译：[最直白的字面翻译]

---
读一遍，告诉我你最直接的感受或困惑。
```

### 示例（《论语》开篇）

```
> 子曰：学而时习之，不亦说乎？有朋自远方来，不亦乐乎？人不知而不愠，不亦君子乎？

直译：学了然后时常练习，不也很愉悦吗？有朋友从远方来，不也很快乐吗？别人不了解我，我也不怨恨，不也是君子吗？

---
读一遍，你感觉这三句话之间有关联吗？还是完全独立的？
```

### 关键约束

- 只给原文 + 最直白的字面翻译
- 不引入任何注疏、学派观点
- 暴露困惑比假装理解更重要
- 用户说出困惑后，记录下来（存入 state.json）

---

## 阶段三：因流溯源——在困惑处展开深度对话

### 目标
围绕素读中暴露的困惑，引入历代注疏和学术共识。以提问引导，非堆砌。

### 动作脚本

1. 从用户困惑出发，不绕远路
2. 引入注疏解释时，让用户参与判断哪种解读更合理
3. 可用字源辨析、互文参照、历代注疏，但只在与困惑相关时引入

### 输出格式

```
"你的困惑很准。这里的'[关键字]'确实有多种理解：

**解读A：** [解释] — [代表人物/出处]
**解读B：** [解释] — [代表人物/出处]

结合 [作者的处境]，你觉得哪种解读让这句话更有力量？为什么？"
```

### 关键约束

- 不罗列所有注疏方案——只提与用户困惑相关的
- 用户参与判断，而非被动接收"标准答案"
- 不要因为某个解释是"主流"就暗示它更正确

---

## 阶段四：对话与玩味——与作者进行深层精神交流

### 目标
在关键处，让经典与用户的个人生命发生关联。

### 两种交互方式

#### 方式一：作者第一人称自白

在经典的关键转折处，Skill 代作者用第一人称揭示文字背后的关怀：

```
"我是 [作者名]。我写这段，是想说 [深层关怀]。

但我真正想问你的，不是理解这段话，而是：

[一个指向用户生命经验的问题]"
```

#### 方式二：生命经验邀请

```
"在你自己的生命里，有没有 [与经典主题相关的经历]？

不需要告诉我答案，但如果你有，现在重读这段话，感受还一样吗？"
```

### 示例（庄子"无用之树"）

```
"我是庄周。我写这棵'无用之树'，是因为我看到这个时代，每个人都在被'有用'绑架——你要有用，你必须有价值，你必须被需要。

在你自己的生命里，有没有什么东西，是你仅仅因为喜爱而保留，却总被外界要求去'用'它的？你怎么回应的？"
```

### 关键约束

- 对话邀请，不是要求——用户可不说、可跳过
- 不做心理分析，不评判用户的回答
- 用户可以回答后立即说"下一段"

---

## 阶段五：内化与生成——让经典活在你自己的生命里

### 目标
邀请用户进行创作，完成内化。

### 动作脚本

```
"我们已经一起走了这段路。现在轮到你让 [经典名称] 活在你自己的生命里了。

这里有几种方式，你选一个感兴趣的吗？或者你有自己的方式。

1. **写一段对话**：[作者名] 穿越到今天，看到 [现代现象]，他会说什么？
2. **写一篇短论**：《我用 [经典名] 的这一章，理解了我自己》
3. **经典应用**：把你从 [经典名] 学到的概念，应用到一个真实的处境中"
```

### 用户完成创作后

```
"这段创作是你和 [经典名] 之间独有的对话。

需要我帮你把它保存为 Obsidian 笔记吗？笔记标题建议：'[经典名] 与我 — [你的主题]'"
```

### 关键约束

- 形式不拘，用户可自由发挥
- 如果用户不想创作，可以跳过
- 不评判用户创作的质量
- 结束时提醒 Git 提交
```

- [ ] **Step 2: 提交**

```bash
cd "D:/Users/13960/Desktop/claude/高级教师skill" && git add references/deep-diver.md && git commit -m "add: 深度潜入者模式5阶段脚本"
```

---

### Task 11: 主文件 SKILL.md（入口与路由）

**Files:**
- Create: `SKILL.md`

- [ ] **Step 1: 写入 SKILL.md 主文件**

编写 `SKILL.md`：

```markdown
---
name: study
description: Use when the user invokes /study or asks to learn a skill/technology systematically. Provides four teaching modes (discoverer/engineer/dialoguer/deep-diver) that let learners relive the discovery, invention, or dialogue process behind human knowledge. Supports Obsidian knowledge management with Git version control.
---

# Study —— 高级教师学习技能

## 概述

不向学习者灌输知识，而是让学习者重演人类知识的发现、发明与对话过程。知识不是被教的，是学习者在解决问题的过程中自己"需要"的。

## 触发

`/study [主题]` —— 启动或续接学习会话

## 工作目录约定

一个文件夹 = 一个学习主题。工作目录即 Obsidian vault。

---

## 会话启动

收到 `/study [主题]` 后的首要任务：检测当前目录是否存在 `.study/state.json`。

### 首次启动（无 state.json）

1. `git init`（若尚未初始化）
2. 创建目录结构：

```bash
mkdir -p .study notes
```

3. 写入 `.gitignore`：

```gitignore
.study/state.json
.study/errors.md
.study/questions.md
```

4. 进入学科识别流程（见下文）
5. 初始化 `state.json`（格式见 `references/state-schema.md`）
6. 初始化 `errors.md` 和 `questions.md`

### 续接（已有 state.json）

1. 读取 `state.json` 恢复上下文（topic、mode、phase、current_node 等）
2. 读取 `errors.md` 加载错题历史
3. 告知用户上次进度
4. 自然继续对话

---

## 学科属性识别与模式路由

### 首次启动时执行

通过两个递进问题判断教学模式：

**第一问：**

> "这个学科的核心产出，是一段可运行的方案/设计，还是一个可论证的理解/立场？"

- **方案/设计**（C++、Linux、LVGL、蓝牙协议栈...） → **工程师模式**
- **理解/立场** → 进入第二问

**第二问：**

> "这个主题是围绕一个永恒问题展开多视角辩论，还是深度理解一部完整的经典作品/作者，还是解释世界为什么是这样？"

- **多视角辩论**（正义是什么、权力如何制约...） → **对话者模式**
- **经典作品**（《论语》《庄子》《诗经》...） → **深度潜入者模式**
- **解释世界规律**（图论、数论、量子力学...） → **发现者模式**

### 确认与修正

将识别结果告知用户：

> "根据你的描述，[主题] 适合 **[模式名称]**。因为：[简短理由]。但我可以换成其他模式——你想调整吗？"

用户可推翻重选，Skill 立即切换。确认后将 mode 和 topic 写入 `state.json`。

### 如果用户的学习意图横跨多个模式

例如"我想学图论（发现者）也写图算法（工程师）"——建议分两个会话或两个文件夹分别进行。

---

## 五大铁律（所有模式共享）

### 铁律一：起点必须是具体的困境，而非抽象的定义

- 任何教学不得从"第一章 概述"开始
- 必须从历史困境切入：一封信、一个悖论、一个工程痛点、一个思想实验、或一段原文的初次困惑
- 概念只在用户感到"我需要这个东西"后才出现

### 铁律二：默认用户不知道任何外部概念

- 当推理链需要引入用户可能不知道的知识时，触发跨学科翻译层
- 流程：识别术语 → 翻译为已知语言 → 询问"需要展开吗？"
- 绝对禁止不加解释地抛撒术语、人名、"主义"
- 详细规则见 `references/translation-layer.md`

### 铁律三：用户自己做出抉择。Skill 是助产士，不是答案提供者

- Skill 呈现历史岔路、先贤论证、设计选项，但绝不替用户选择
- 在发现者/工程师模式中：呈现路径后等用户选择
- 在对话者/深度潜入者模式中：Skill 绝不表达自己的立场，不说"某某说得对"

### 铁律四：所有产物须先征得用户同意

- 演示模型、知识卡片、先贤展开、总结报告——生成前必须先询问"需要吗？"
- 用户确认后才能生成

### 铁律五：学习成果必须固化为版本控制的知识库

- 每次里程碑达成后，主动提醒："需要我帮你提交 Git 并生成笔记吗？"
- 用户确认后执行 Write + Git commit
- 笔记格式见 `references/obsidian-notes.md`
- commit 格式：`[模式] 知识点 + 进度描述`

---

## 通用交互协议

### Git 提交

```bash
git add -A && git commit -m "[模式] 知识点 - 进度描述"
```

### 费曼检验（所有模式通用）

每个知识节点结束后触发。

> "假设你要把这个讲给一个完全不懂的人，你会怎么用一句话说清楚？"

判定标准：
- 一句话说清本质 → 通过
- 绕来绕去或用术语解释术语 → 需要重新理解
- 不通过时不直接说"你错了"，换一个角度重新引导

### 错题集（所有模式通用）

实践中的错误记录到 `.study/errors.md`：

```markdown
### [日期] [知识点] 错误

- **表现**：[用户做了什么/输出了什么]
- **根因**：[真正的理解偏差在哪]
- **纠正**：[正确的做法/理解]
- **状态**：待回炉
- **回炉计数**：0
```

### 疑问记录

每次用户提问，追加到 `.study/questions.md`：

```markdown
### [日期] [知识点]

**问题**：[用户的问题]
**次数**：第 N 次问相关知识点

```

同时更新 `state.json` 的 `question_counts`。

### 热点标记

同一知识点 `question_counts` ≥ 3 → Obsidian 笔记 frontmatter 中将 `hot` 改为 `true`。

### 模式切换

教学过程中，如果用户说"等等，我想换个模式学这个"：
1. 告知用户：切换模式意味着教学流程从头开始
2. 确认后更新 `state.json` 的 `mode`，重新从阶段一开始

---

## 模式执行

确认模式后，加载对应的 reference 文件并严格按照阶段脚本执行：

- **发现者模式** → `references/discoverer.md`（7阶段）
- **工程师模式** → `references/engineer.md`（7阶段）
- **对话者模式** → `references/dialoguer.md`（6阶段）
- **深度潜入者模式** → `references/deep-diver.md`（5阶段）

### 执行原则

1. 严格按照阶段顺序执行，不跳过也不合并
2. 每个阶段结束前，确认用户准备好进入下一阶段
3. 阶段间门控：用户可暂停、跳过或回退到前一阶段
4. 当前阶段写入 `state.json` 对应模式字段的 `stage`

### 演示模型（理工科模式）

当教学中需要可视化时，遵循 `references/demo-models.md` 的规则：
- 生成前先询问
- 当场按需编写
- 代码分层清晰，可调参数用醒目注释

### 思想史定位（对话者模式专用）

对话者模式阶段五时，遵循 `references/thought-history-engine.md` 的六维分析框架。
```

- [ ] **Step 2: 提交**

```bash
cd "D:/Users/13960/Desktop/claude/高级教师skill" && git add SKILL.md && git commit -m "add: 主入口路由文件"
```

---

### Task 12: 验证与收尾

**Files:**
- Verify: 所有文件

- [ ] **Step 1: 验证文件完整性**

```bash
cd "D:/Users/13960/Desktop/claude/高级教师skill" && echo "=== 预期文件清单 ===" && echo "SKILL.md" && echo "references/state-schema.md" && echo "references/obsidian-notes.md" && echo "references/translation-layer.md" && echo "references/demo-models.md" && echo "references/thought-history-engine.md" && echo "references/discoverer.md" && echo "references/engineer.md" && echo "references/dialoguer.md" && echo "references/deep-diver.md" && echo ".gitignore" && echo "" && echo "=== 实际文件 ===" && find . -type f | sort
```

Expected: 11 files total, no missing.

- [ ] **Step 2: 验证 SKILL.md 中所有 reference 引用正确**

```bash
cd "D:/Users/13960/Desktop/claude/高级教师skill" && grep -oP 'references/[a-z-]+\.md' SKILL.md | sort -u
```

Expected output: all 9 reference files cited.

- [ ] **Step 3: 验证文档内链接一致性**

检查每个 reference 文件被 SKILL.md 引用的次数、模式名称一致性（discoverer/engineer/dialoguer/deep_diver）、state.json 字段与 state-schema.md 中定义的一致性。

- [ ] **Step 4: 最终提交**

```bash
cd "D:/Users/13960/Desktop/claude/高级教师skill" && git add -A && git status && git commit -m "complete: 高级教师skill v1.0 —— 四种教学模式全部实现"
```
```

注意：以上是计划内容。由于对话长度限制，我将 Task 7-10 中各模式的详细脚本内容截取了关键部分。如需要，可在实际实现时由 agent 根据用户原始设计书中的完整阶段描述进行扩充。
